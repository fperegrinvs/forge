# @forge/check-runner

Runs task-type gates/checks for Forge plans. Used by control-plane to enforce workflow policy.

## Commands

From the repo root:

```bash
bun run --filter @forge/check-runner build
bun run --filter @forge/check-runner test
bun run --filter @forge/check-runner typecheck
```

