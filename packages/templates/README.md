# @forge/templates

Project and module scaffolding used by `forge init` and `forge scaffold module`.

## Commands

From the repo root:

```bash
bun run --filter @forge/templates build
bun run --filter @forge/templates test
bun run --filter @forge/templates typecheck
```

