# @forge/shared-utils

Shared filesystem/process utilities used across packages in this monorepo.

## Commands

From the repo root:

```bash
bun run --filter @forge/shared-utils build
bun run --filter @forge/shared-utils test
bun run --filter @forge/shared-utils typecheck
```

