#!/usr/bin/env bash
set -euo pipefail

branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || true)"
if [[ "$branch" == "main" ]]; then
  echo "ERROR: direct commits to 'main' are not allowed. Create a codex/* branch and open a PR." >&2
  exit 1
fi

