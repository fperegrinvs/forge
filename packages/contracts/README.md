# @forge/contracts

Plan schema, types, validators, and migration tooling for Forge plan files.

## Commands

From the repo root:

```bash
bun run --filter @forge/contracts build
bun run --filter @forge/contracts test
bun run --filter @forge/contracts typecheck
```

