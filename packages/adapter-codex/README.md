# @forge/adapter-codex

Codex adapter implementation for the Forge control-plane. Provides a unified run/event surface for orchestration.

## Commands

From the repo root:

```bash
bun run --filter @forge/adapter-codex build
bun run --filter @forge/adapter-codex test
bun run --filter @forge/adapter-codex typecheck
```

