# @forge/adapter-claude

Claude adapter implementation for the Forge control-plane. Provides a unified run/event surface for orchestration.

## Commands

From the repo root:

```bash
bun run --filter @forge/adapter-claude build
bun run --filter @forge/adapter-claude test
bun run --filter @forge/adapter-claude typecheck
```

