{
  "name": "forge-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.8",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "preflight": "./scripts/bootstrap-toolchain.sh --check",
    "hooks:install": "./scripts/install-githooks.sh",
    "workflow:sync": "bun scripts/sync-workflow-assets.mjs",
    "workflow:check-sync": "bun scripts/check-workflow-sync.mjs",
    "workflow:check": "bun run --filter @forge/control-plane build && node scripts/workflow-check.mjs --plan plans/forge-monorepo-v1.plan.json",
    "build": "bun run --workspaces build --if-present",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "lint:rust": "cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml -- -D warnings",
    "test:rust": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:desktop": "vitest run apps/desktop/src/**/*.test.ts",
    "verify": "bun run workflow:check-sync && bun run docs:check && bun run typecheck && bun run lint && bun run architecture:check && bun run test:coverage && bun run workflow:check",
    "dev:desktop": "bun run --filter @forge/desktop dev",
    "dev:desktop:tauri": "./scripts/dev-desktop-tauri.sh",
    "dev:desktop:tauri:oneshot": "FORGE_DESKTOP_ONESHOT=1 ./scripts/dev-desktop-tauri.sh",
    "dev:desktop:tauri:noreload": "./scripts/dev-desktop-tauri-noreload.sh",
    "docs:check": "bun scripts/docs-check.mjs",
    "architecture:check": "bun scripts/architecture-check.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.0.8",
    "eslint": "^9.22.0",
    "eslint-plugin-import": "^2.31.0",
    "jiti": "^2.6.1",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.25.0",
    "vitest": "^3.0.8"
  }
}
